<h4>{{{t "assessment:common.title" title=title}}}</h4>
<p>{{t "assessment:assessment_progressbar.label" completed=completed count=total}}</p>

<div class="toolbar">
    <a href="{{t "common:menu.tutorial.href"}}" class="btn btn-primary" role="button">{{t "common:menu.tutorial.label"}}</a>
    <a href="{{t "common:menu.judge.href"}}" class="btn btn-primary" role="button">{{t "common:menu.judge.label"}}</a>
</div>

<!--
    <div class="progress">
        <div class="progress-bar progress-bar-info progress-bar-striped"
             role="progressbar" aria-valuenow="{{completedNum}}" aria-valuemin="0" aria-valuemax="{{comparisonsNum}}"
             data-toggle="tooltip" data-placement="bottom" title="{{t "assessment:comparisons.progressbar.tooltip"}}"
             style="width:{{percentage}}%"
             >
            {{#if labelLeft }}
                <span>{{t "assessment:comparisons.progressbar.label" completed=completedNum count=comparisonsNum}}</span>
            {{/if}}
        </div>
        {{#if labelRight}}
            <span>&nbsp;{{t "assessment:comparisons.progressbar.label"  completed=completedNum count=comparisonsNum}}</span>
        {{/if}}
    </div>
-->
