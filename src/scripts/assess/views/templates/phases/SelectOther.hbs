<div class="col-sm-12 column">
    <div class="well">
        <h4>{{title}}</h4>
        <p>{{description}}</p>
        <button id="select-other-A-button" type="button" class="btn btn-primary"
                data-sending-text="{{t "assess:common.sending"}}"
                data-selection-id="{{representations.a._id}}">
            {{t "assess:phase_select-other.a_button.label"}}
        </button>
        <button id="select-other-B-button" type="button" class="btn btn-primary"
                data-sending-text="{{t "assess:common.sending"}}"
                data-selection-id="{{representations.b._id}}">
            {{t "assess:phase_select-other.b_button.label"}}
        </button>
    </div>
</div>

