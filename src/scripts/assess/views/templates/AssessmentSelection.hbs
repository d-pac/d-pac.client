<div class="row">
    <div class="col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3 col-lg-4 col-lg-offset-4 column">
        <div id="assess-selection-view" class="panel panel-default">
            <div class="panel-heading">
                <h4>{{t "assess:assessment_selection.title"}}</h4>
            </div>
            <div class="panel-body">
                {{#if items.length }}
                    <p>{{t "assess:assessment_selection.description"}}</p>
                    {{#each items}}
                        <button type="button" class="btn btn-primary btn-block assessment-button"
                                data-model-id="{{_id}}"
                                data-sending-text="{{t "assess:assessment_selection.submit_button.sending"}}">
                            {{title}} ({{progress.completedNum}}/{{progress.total}})
                        </button>
                    {{/each}}
                {{else}}
                    <p>{{t "assess:assessment_selection.empty"}}</p>
                {{/if}}
            </div>
        </div>
    </div>
</div>
