'use strict';

var debug = require( 'debug' )( 'dpac:assess.controllers', '[<%= file.name %>]' );

module.exports = Marionette.Controller.extend( {
    initialize : function(){
        debug( '#initialize' );

    }
});
